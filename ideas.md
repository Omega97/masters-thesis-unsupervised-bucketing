
- *task vector* -> *sample gradient*
    
- The dispatcher needs only to be called on half of the activations of the dual *accumulator layer*, making it 128x3 parameters in size.
    
- **certi algoritmi di clustering decidono $B$** 
    
- U Map in 3D, DBScan, Density Peak Clustering
    
- PCA per aiutare il clustering?
    
- visualizzare i cluster; plot per la tesi
