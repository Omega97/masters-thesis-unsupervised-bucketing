# 2026-08-29

## Context

First daily note for this repo. Session focused on turning the UniTS thesis from a template into a usable scaffold: structure, abstract, and related-work pointers. Working title: *Unsupervised State Bucketing for Mixture of Experts in Resource-Constrained Chess Engines*.

## Plan for today

1. Freeze a chapter outline from `thesis-structure.md` into the LaTeX tree.
2. Draft / paste the abstract as a single block of text.
3. Collect related-work notes.

## Execution log

- Mapped `thesis-structure.md` into numbered chapters and sections in LaTeX, with gray `\draftnote` placeholders for what each section should contain.
- Renamed the master file to `main.tex` and split the body into 9 files under `THESIS/thesis_template-master/chapters/` (`\include` for chapters, `\input` for frontmatter snippets). Compile target is **only** `main.tex` → `out/main.pdf` (copy at repo root: `main.pdf`).
- Front page already filled from `thesis-info.md` (Omar Cusma Fait, SM3800018, DSAI / MIGe, Ansuini + Zennaro/ICTP, session 23 Oct 2026).
- Pasted `THESIS/0 - Abstract.md` into `chapters/abstract.tex` as one block; section labels are `%` comments only. Later revision applied (gap vs activation clustering; scraps / expected results left out of the PDF).
- Related-work notes: `related-work.md` at repo root (survey still incomplete; abstract flags a literature gap on sample-gradient bucketing).
- README: list of links to important root files at the top.

## Carry-over

- Write real prose for Ch. 1–7 (replace `\draftnote` scaffolding).
- Dig deeper in the literature for methods close to sample-gradient clustering (see scraps in `THESIS/0 - Abstract.md`).
- Fill related-work chapter from `related-work.md`.
- Experiments / Elo / MoE results still `#todo`.
- Dedication / bibliography still placeholders.
- Confirm with supervisor whether Zennaro stays as internship supervisor vs official co-supervisor.
